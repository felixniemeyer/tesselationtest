#version 430

layout(triangles, equal_spacing, cw) in;

in vec3 tcPosition[];
out vec3 tePosition;

uniform mat4 mvp;

void main()
{
	tePosition = gl_TessCoord + tcPosition[0];
	gl_Position = mvp * vec4(tePosition, 1);
}